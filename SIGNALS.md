# Как формируются арбитражные сигналы

## Обзор системы

Система CryptoArb анализирует цены на 8 криптобиржах в реальном времени и находит разницу цен (спред) для получения прибыли.

---

## Этап 1: Сбор данных с бирж

### Подключение через WebSocket

Каждая биржа подключается через WebSocket для получения данных в реальном времени:

```
Bybit    → wss://stream.bybit.com/v5/public/spot
OKX      → wss://ws.okx.com:8443/ws/v5/public  
MEXC     → wss://wbs.mexc.com/ws
Bitget   → wss://ws.bitget.com/spot/v1/stream
Gate.io  → wss://api.gateio.ws/ws/v4/
KuCoin   → wss://ws-api-spot.kucoin.com
CoinEx   → wss://socket.coinex.com/v2/spot
BingX    → wss://open-api-ws.bingx.com/market
```

### Получаемые данные (Ticker)

Для каждой пары получаем:
- **bid** - лучшая цена покупки (по которой можно ПРОДАТЬ)
- **ask** - лучшая цена продажи (по которой можно КУПИТЬ)
- **last** - последняя цена сделки
- **volume24h** - объём торгов за 24 часа

---

## Этап 2: Анализ спреда

### Алгоритм поиска арбитража

Каждую секунду система анализирует все включённые пары:

```javascript
// Для каждой пары (например BTC/USDT):

1. Собираем цены ASK со всех бирж:
   - Bybit:  $67,450
   - OKX:    $67,445  ← Минимальная (покупаем здесь)
   - MEXC:   $67,480
   - Bitget: $67,520  ← Максимальная
   
2. Собираем цены BID со всех бирж:
   - Bybit:  $67,440
   - OKX:    $67,435
   - MEXC:   $67,470
   - Bitget: $67,510  ← Максимальная (продаём здесь)

3. Вычисляем спред:
   Спред = ((BID_max - ASK_min) / ASK_min) × 100
   Спред = (($67,510 - $67,445) / $67,445) × 100
   Спред = 0.096% (грязный, без учёта комиссий)
```

---

## Этап 3: Расчёт прибыли

### Учёт комиссий бирж

Каждая биржа берёт комиссию за сделку:

| Биржа   | Maker Fee | Taker Fee |
|---------|-----------|-----------|
| Bybit   | 0.10%     | 0.10%     |
| OKX     | 0.08%     | 0.10%     |
| MEXC    | 0.00%     | 0.10%     |
| Bitget  | 0.10%     | 0.10%     |
| Gate.io | 0.20%     | 0.20%     |
| KuCoin  | 0.10%     | 0.10%     |
| CoinEx  | 0.20%     | 0.20%     |
| BingX   | 0.10%     | 0.10%     |

### Формула чистой прибыли

```javascript
// Пример: Покупка на OKX, продажа на Bitget

buyFee = 0.10%   // OKX taker fee
sellFee = 0.10%  // Bitget taker fee
totalFees = 0.20%

grossSpread = 0.096%
netProfit = grossSpread - totalFees
netProfit = 0.096% - 0.20%
netProfit = -0.104% ← УБЫТОК! Сигнал не генерируется.
```

### Когда генерируется сигнал

Сигнал создаётся только если:

```javascript
netProfit >= minProfitPercent  // По умолчанию 1%
```

---

## Этап 4: Структура сигнала

Когда найдена прибыльная возможность, создаётся объект:

```javascript
{
  id: "uuid-уникальный-id",
  pair: "SOL/USDT",
  
  // Где покупать
  buyExchange: "mexc",
  buyPrice: 142.35,
  buyFee: 0.14,        // В долларах при $1000 сделке
  
  // Где продавать  
  sellExchange: "okx",
  sellPrice: 144.20,
  sellFee: 0.14,
  
  // Аналитика
  spreadPercent: 1.30,  // Грязный спред
  profitPercent: 1.10,  // Чистая прибыль после комиссий
  estimatedProfit: 11,  // $11 при сумме сделки $1000
  
  timestamp: 1701388800000
}
```

---

## Этап 5: Отправка в Telegram

### Формат сообщения

```
🔥 Arbitrage Alert!

📊 Pair: SOL/USDT
📈 Profit: +1.10% (~$11.00)

🟢 Buy: MEXC
   └ Price: $142.35
   └ Fee: $0.14

🔴 Sell: OKX
   └ Price: $144.20
   └ Fee: $0.14

📉 Spread: 1.30%

⏰ 12:30:45

━━━━━━━━━━━━━━━━━━━━
⚠️ DYOR - Verify prices before trading!
```

---

## Как входить в сделку

### Шаг 1: Подготовка

1. Иметь USDT на обеих биржах (на MEXC для покупки, на OKX для маржи/залога)
2. Или иметь актив на бирже продажи

### Шаг 2: Исполнение (за 10-30 секунд)

```
ОДНОВРЕМЕННО:
├── На MEXC: Лимитный ордер BUY SOL/USDT по $142.35
└── На OKX:  Лимитный ордер SELL SOL/USDT по $144.20
```

### Шаг 3: Перенос активов (позже)

После исполнения ордеров:
- Вывести SOL с MEXC на OKX
- Или: Вывести USDT с OKX на MEXC

### Риски

1. **Проскальзывание** - цена может измениться за время исполнения
2. **Частичное исполнение** - ордер может исполниться не полностью
3. **Сетевые комиссии** - вывод крипты стоит денег
4. **Время вывода** - блокчейн-подтверждения занимают время

---

## Настройка системы

### Минимальный порог прибыли

```
Settings → Min Profit: 1.0%
```

Рекомендации:
- **0.5%** - Много сигналов, но низкая прибыль
- **1.0%** - Баланс (рекомендуется)
- **1.5%** - Меньше сигналов, но надёжнее
- **2.0%+** - Редкие, но очень прибыльные сигналы

### Сумма сделки

```
Settings → Trade Amount: $1000
```

Расчёт прибыли в долларах:
```
estimatedProfit = (profitPercent / 100) × tradeAmount
```

При $1000 и прибыли 1.5% → $15

---

## MEXC API для автоматизации

### Подключение

Установите переменные окружения:
```
MEXC_API_KEY=ваш_api_key
MEXC_SECRET_KEY=ваш_secret_key
```

### Получение API ключей

1. Войдите на mexc.com
2. Профиль → API Management
3. Создайте новый API ключ
4. Включите права: Read + Spot Trade
5. Добавьте IP в whitelist (опционально)

### Пример использования

```javascript
import { mexcApi } from './services/mexc-api';

// Получить цену
const ticker = await mexcApi.getTicker("SOL/USDT");
console.log(`SOL: bid=${ticker.bidPrice}, ask=${ticker.askPrice}`);

// Получить баланс
const balance = await mexcApi.getBalance("USDT");
console.log(`USDT: ${balance.free} свободно`);

// Разместить ордер
const order = await mexcApi.placeOrder({
  symbol: "SOL/USDT",
  side: "BUY",
  type: "LIMIT",
  quantity: "0.5",
  price: "142.35"
});
```

---

## Cooldown между сигналами

Чтобы не спамить одинаковыми сигналами:

```javascript
COOLDOWN_MS = 30000  // 30 секунд

// Ключ для отслеживания:
opportunityKey = `${pair}-${buyExchange}-${sellExchange}`
// Пример: "SOL/USDT-mexc-okx"

// Новый сигнал отправляется только если прошло 30+ секунд
// с последнего такого же сигнала
```

---

## Логирование

В интерфейсе отображаются:

- 📊 **Анализ** - текущие цены и спреды
- 🔥 **Арбитраж** - найденные возможности
- 📨 **Сигнал** - отправка в Telegram
- ✅ **Подключение** - статус бирж
- ⚠️ **Предупреждение** - проблемы
- ❌ **Ошибка** - критические ошибки
